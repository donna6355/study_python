CREATE TABLE MEMBER(
	ID VARCHAR(100) NOT NULL,
	PW VARCHAR(100) NOT NULL,
	NICK VARCHAR(100) NOT NULL,
	PHONE VARCHAR(100) NOT NULL,
	PRIMARY KEY(ID)
);

INSERT INTO MEMBER VALUES ('admin','1234','관리자','02-1234-1234');

COMMIT;

SELECT * FROM MEMBER;